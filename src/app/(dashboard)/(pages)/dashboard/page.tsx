import { DashboardPageContent } from '.';

export const metadata = {
  title: 'Dashboard',
  description: 'Página do dashboard',
};

export default async function DashboardPage() {
  return <DashboardPageContent />;
}
