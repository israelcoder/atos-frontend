import { TicketPageContent } from '.';

export const metadata = {
  title: `Ticket`,
  description: `Lista de chamados`,
};

export default async function TicketPage() {
  return <TicketPageContent />;
}
